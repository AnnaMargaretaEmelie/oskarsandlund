export default function Home() {
  return (
    <div>
      <section aria-labelledby="home-hero-heading">
        <h2 id="home-hero-heading">Hero</h2>
      </section>
      <section aria-labelledby="home-bio-heading">
        <h2 id="home-bio-heading">Bio</h2>
      </section>
      <section aria-labelledby="home-featured-credits-heading">
        <h2 id="home-featured-credits-heading">Featured credits</h2>
      </section>
    </div>
  );
}
